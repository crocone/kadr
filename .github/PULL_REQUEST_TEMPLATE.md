## What this changes

<!-- One or two sentences. Link the issue if there is one. -->

## Checks

- [ ] `npm run typecheck`, `npm run lint`, `npm test` and `npm run build` pass
- [ ] No new permission in the base set — anything added is in `optional_permissions` and
      requested when the feature is switched on
- [ ] No new network request that the user did not explicitly trigger
- [ ] New assets are recorded in `LICENSES.md`
- [ ] Visual features go through the `Doc` model, so preview and export stay identical
